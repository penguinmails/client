export interface DbLeadList {
  id: string;
  name: string;
  contacts: number;
  description: string;
}

export interface DbLeadListRow {
  id: string;
  name: string;
  contacts: number;
  description: string;
}
