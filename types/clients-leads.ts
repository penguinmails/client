/**
 * Leads types backward compatibility layer
 * 
 * @deprecated Import from '@features/leads' instead
 */

// Re-export everything from the new location
export * from '@features/leads/types';
export * from '@features/leads/types/legacy-leads';
